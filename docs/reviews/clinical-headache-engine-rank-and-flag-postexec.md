# Clinical review — headache engine rank-and-flag (POST-EXECUTION gate)

**Decision:** approve
**Reviewer:** clinical-reviewer (model: Claude Opus 4.8)
**Date:** 2026-06-04
**Gate type:** Class E POST-EXECUTION clinical gate — fidelity check of the committed-but-uncommitted engine + test diff against the pre-execution spec and the 10 binding conditions. Pairs with the pre-execution gate `docs/reviews/clinical-headache-engine-rank-and-flag.md`.
**Reviewed:** implemented engine (`src/data/clinicHeadacheData.ts`, 1096 lines) + regression contract (`src/data/clinicHeadacheData.test.ts`, 1195 lines) + the pre-execution clinical gate (10 conditions) + the medsci authoring spec + the architect §17.1 + `ichd3-2018` registry entry. Read-only (Read/Grep/Glob; no web).

## Scope
- **Claims touched:** the nine registered ICHD-3 clinic-headache claims, all mapped to `ichd3-2018`. No claim *text* changed. No new claim IDs. The refactor changes which phenotypes surface and how they are flagged — not the wording of any registered claim.
- **Citations affected:** `ichd3-2018` only. No new citation. No `last_reviewed` refresh triggered by this PR.
- **Surfaces changed:** computed strings from the matching engine (`evaluateHeadachePhenotypes`). New `PhenotypeMatch` fields `definitionallyExcluded: boolean` + `exclusionReason?: string`. Three new chips (`aura-symptom-unilateral`, `attacks-ge-2`, `migraine-history-established`) added to the chip vocabulary. No JSX, no result-sentence, no tooltip-prose change in this PR (Stage Two).
- **Evidence-verifier packet:** not applicable (no trial data change; ICHD-3 already registered, within window).
- **Trial-statistician report:** not applicable (no trial, no statistic).

## Semantic validity

The code faithfully encodes the approved spec on all ten verification points. This was a line-by-line fidelity check, not a re-litigation of the settled clinical classification.

**1 — Role classification matches the approved 40-criterion map. CONFIRMED.** Spot-checked every load-bearing `role:` field against the medsci summary (spec §1, L137-139):
- *scorable (16):* mig-A (L534), mig-C (L540), aura-A (L566), aura-C (L574), cm-B (L766), tth-A (L590), tth-C (L595), ctth-C (L623), cluster-A (L647), cluster-D (L659), ph-A (L790), ph-D (L799), hc-B (L681), sunct-A (L817), sunct-D (L828), vm-B (L734) — all `role: 'scorable'`. ✓
- *demote-gate (10):* mig-B (L538), mig-D (L543), tth-B (L593), ctth-B (L621), cluster-B (L651), cluster-C (L656), ph-B (L793), ph-C (L796), sunct-B (L820), hc-C (L685) — all `role: 'demote-gate'`. ✓ The four `*-C`/`hc-C` criteria that moved from the brief's provisional `*-B`-only list (cluster-C, ph-C, hc-C) are correctly demote, and the eight former `definitional:true` demotions all carry demote semantics.
- *suppress-gate (14):* aura-B (L570), cm-A (L762), cm-C (L773), tth-D (L601), ctth-A (L618), ctth-D (L630), sunct-C (L826), hc-A (L679), hc-D (L688), ph-E (L802), ndph-A (L705), ndph-B (L710), vm-A (L729), vm-history (L740) — all `role: 'suppress-gate'`. ✓
The `definitional` field appears zero times in the engine (arch follow-up 1 checkable met); `role` is non-optional on `Criterion` (L149).

**2 — Three minimum-evidence floors match the approved chip sets. CONFIRMED.**
- §1.1 `migraineEvidenceFloor` (L475-480): requires ≥1 C feature (cCount > 0) AND (cCount + dCount) ≥ 2, where `MIGRAINE_C_CHIPS` = {loc-unilateral, qual-pulsating, sev-moderate, sev-severe, act-aggravated} and `MIGRAINE_D_CHIPS` = the five §1.1 D symptoms. Exactly the approved "≥2 migraine-pointing incl ≥1 C" rule (condition 1, spec §2). ✓
- §2.2 `tthEvidenceFloor` (L487-498): requires ≥1 §2.2 C feature AND ≥2 total TTH-pointing chips, `TTH_C_CHIPS` = {loc-bilateral, qual-pressing-tightening, sev-mild, sev-moderate, act-not-aggravated}. Matches condition 2 (upgraded to mandatory). ✓
- §3.1 `clusterEvidenceFloor` (L512-517): requires ≥1 defining feature from `CLUSTER_C_DEFINING_CHIPS` = [loc-unilateral, loc-orbital-temporal, sev-severe, sev-very-severe, dur-15-to-180-min] AND ≥2 total cluster-pointing chips. **This defining set is clinically defensible as the §3.1 floor.** It does not let cluster surface off one incidental chip (one autonomic chip alone fails — test H L764-770 confirms `sym-autonomic-ipsilateral` alone is absent because autonomic is *not* a defining feature). It does not exclude a genuine near-miss: the canonical between-bouts cluster patient (unilateral orbital severe pain) clears it on location+severity even before duration is confirmed (test S4 L995-1011 surfaces as Probable cluster). The defining set is precisely the union of cluster-B's discriminating chips, which is the right floor — it gates on the constellation that distinguishes cluster (unilateral + orbital + severe + short) rather than the non-specific autonomic/restlessness/attack-count chips. Defensible. ✓

**3 — `exclusionReason` introduces no new clinical claim. CONFIRMED.** The EMIT branch (L975-992) sets `exclusionReason: suppressGateFailed.label` and `missingCriteria: [{ ... label: suppressGateFailed.label, description: suppressGateFailed.description }]` — it reuses the existing criterion label/description strings already in the engine, authoring no new clinical sentence. This is the only site where `exclusionReason` is assigned (verified — single assignment, L991). The EMIT set is exactly {tth-D, ctth-D, cm-C} (`EMIT_CRITERION_IDS`, L868). Conditions 5 + 6 met.

**4 — Engine output carries no band words, no percentage, no diagnosis label. CONFIRMED.** `definitionallyExcluded` is a plain boolean; `exclusionReason` is a reused criterion label. The EMIT match sets `matchStrength: 'none'` (L984), so an excluded phenotype is never an active match. No "Leading/Possible/Less likely", no "%", no diagnosis sentence anywhere in the emitted shape. `displaySection` for the probable branch resolves a §X.5 *section reference* (e.g. "ICHD-3 §1.5 Probable migraine"), which is an ICHD-3 structural label, not a band word — consistent with the pre-execution ruling and arch follow-up 2. Condition 6 met.

**5 — SNNOOP10 backstop preserved. CONFIRMED.** `RED_FLAG_CHIPS` (L403-408) and `anyRedFlagActive` (L410-413) are untouched — identical to the pre-refactor backstop. The eval loop (L924-1081) does not read, alter, or short-circuit on red-flag chips; red-flag rendering is the page's responsibility and is unchanged. Condition 9 met. (Note: the engine itself never gated on red flags — the non-collapsible SNNOOP10 render is a page concern; the engine's red-flag helper is intact and the refactor leaves it alone, which is the correct scope.)

**6 — Aura-laterality rewire — the NON-NEGOTIABLE condition 10. CONFIRMED CORRECT.** `auraCharacteristicCount` (L448-455) counts {aura-spread-ge-5min, aura-multi-symptoms-succession, aura-each-5-to-60min, aura-positive-symptoms, aura-headache-within-60min} + `(has(s, 'aura-symptom-unilateral') ? 1 : 0)`. It does **NOT** reference `loc-unilateral`. I grepped every `loc-unilateral` occurrence in the engine: it survives ONLY where headache-pain laterality is genuinely meant — `migraineCharacterCount` (L422), `MIGRAINE_C_CHIPS` floor (L473), `CLUSTER_C_DEFINING_CHIPS` floor (L506), cluster-B (L651), hc-A (L679), ph-B (L793), sunct-B (L820) — plus the type union (L38), the chip definition (L269), and three explanatory comments. This is exactly the approved allow-list. `aura-C`'s `contributingChips` (L574) correctly lists `aura-symptom-unilateral` (not `loc-unilateral`), and the new chip is defined in the `aura` group (L321) with §1.2-C-item-4 teach text. The test at L1105-1134 directly asserts the rewire: aura surfaces full on `aura-symptom-unilateral` as the 3rd characteristic, and `loc-unilateral` alone (without `aura-symptom-unilateral`) does NOT reach full. The defect that would have manufactured a wrong-chip near-miss prompt is closed. ✓

**7 — vm-history DROPS, and vm-B relabeled to A1.6.6 criterion C. CONFIRMED.** `vm-history` (L740) is `role: 'suppress-gate'` and its id is NOT in `EMIT_CRITERION_IDS`, so it drops silently — consistent with vm-A and condition 4. `vm-B` (L730-734) label/description now read "Migrainous features during ≥50% of vertigo episodes (A1.6.6 C)" / "Appendix A1.6.6 criterion C — …"; `evaluate` and `contributingChips` unchanged, `role: 'scorable'` retained. The historical mislabel is corrected; no new source needed (A1.6.6 carries criterion B directly, per the verbatim `quoted_text` and the medsci/pre-execution ratification). ✓

**8 — Emit/drop split correct. CONFIRMED.** `EMIT_CRITERION_IDS = {tth-D, ctth-D, cm-C}` (L868). In the eval loop (L973-997): if a suppress-gate fails AND its id is in EMIT, the phenotype is pushed with `definitionallyExcluded: true` + `matchStrength: 'none'`; otherwise `continue` (silent drop). The X.5 exclusion sweep (L1063-1068) and sort (L1072-1078) both explicitly leave `matchStrength: 'none'` / `definitionallyExcluded` entries unpromoted and last. All eleven DROP-set suppressions (aura-B, cm-A, ctth-A, sunct-C, hc-A, hc-D, ph-E, ndph-A, ndph-B, vm-A, vm-history) drop silently. ✓ Tests confirm: H1/H2 emit with `definitionallyExcluded:true` + truthy `exclusionReason` + non-active strength (L781-809); cm-C emits (L811-823); the DROP-set tests assert absence (L827-878).

**9 — H6 VM-history test inversion is clinically correct. CONFIRMED.** The inverted test (L708-716) asserts VM is ABSENT on `vest-vertigo-migrainous + sym-phonophobia` *without* `migraine-history-established`; the paired test (L718-727) asserts VM SURFACES once `migraine-history-established` is added. This encodes A1.6.6 criterion B exactly: VM by definition requires an established 1.1/1.2 migraine history, which distinguishes it from BPPV/Menière/vestibular paroxysmia. The prior test asserted VM surfaces on vertigo+phonophobia alone — that was the over-surfacing the new gate closes. The inversion is the right clinical rule. ✓

**10 — Dev-time invariant. CONFIRMED.** Two implementations, both correct and consistent: (a) the module-load throw in the engine (L885-898) iterates every phenotype and throws unless it has a `suppress-gate` criterion OR `hiddenUntilTrial` OR membership in `PHENOTYPES_WITH_EVIDENCE_FLOOR` = {migraine-without-aura, episodic-tth, cluster-headache}; (b) the test mirror (L648-663) asserts the same. This is the structural guarantee that no feature-only phenotype can surface off one incidental chip (condition 7 / arch follow-up 4). §1.1 passes via floor, §2.2 via floor + tth-D, §3.1 via floor, §1.2 via aura-B, §1.3 via cm-A/cm-C, §2.3 via ctth-A/ctth-D, §3.2/§3.4 via ph-E/hc-D + hiddenUntilTrial, §3.3 via sunct-C, §4.10 via ndph-A/ndph-B, VM via vm-A/vm-history. Every phenotype has a designated suppression path. ✓

## Citation accuracy

`ichd3-2018` registry entry (registry.ts L1365-1386): current version (2018, third edition); PMID 29368949 and url ichd-3.org present; `section` lists all 11 instantiated sections including A1.6.6. The verbatim `quoted_text` (L1385) carries §1.1, §1.2 (incl. C item 4 "at least one aura symptom is unilateral"), §1.3 (incl. Note 1), §2.2, §2.3 D (verbatim ≤1-pool), §3.1-§3.4, §4.10, and the A1.6.6 research-criteria note — I verified the code's criterion encodings against this quoted text directly:
- §1.2 C item 4 verbatim "at least one aura symptom is unilateral" → matches the `aura-symptom-unilateral` rewire (the entire basis of condition 10). ✓
- §1.2 A "at least two attacks" → matches the `attacks-ge-2` rewire of aura-A. ✓
- §2.3 D verbatim "(1) no more than one of photophobia, phonophobia or mild nausea; (2) neither moderate or severe nausea nor vomiting" → matches ctth-D's evaluator (L630). ✓
- §3.1 B "15-180 minutes", §3.2 B "2-30 minutes", §3.3 B "1-600 seconds", §4.10 B "within 24 hours" → all match their respective duration/onset chips. ✓
The registry does NOT carry verbatim §1.5, §3.5, or A1.6.6 criterion B; per the standard authoring/gating division of labor (clinical-reviewer is read-only, no web), those rest on the medical-scientist's WebFetch, which was ratified at the pre-execution gate. I did not re-verify them against the web — I cannot, and the task brief explicitly scopes them out of this pass. The code's use of them (the §X.5 Probable framework labels, the A1.6.6-B migraine-history gate) is internally consistent with the ratified spec.

## Editorial / expert context

Not applicable — no new trial entry in this PR. (Mandatory-block #8 governs new-trial-entry evidence packets; this is a guideline-criteria logic refactor with no trial-data change.)

## Freshness

`ichd3-2018`: `last_reviewed: 2026-05-25`, `review_window_months: 24` (registry.ts L1383-1384) → within window as of 2026-06-04. **Pass.** No refresh required by this change; the §13.6 six-step refresh was completed at the 2026-05-25 rebuild and is unaffected by this logic-only PR.

## Rationale

The implementation faithfully encodes every clinically-approved decision from the pre-execution gate and the authoring spec. All ten verification points pass on a line-by-line reading: the 40-criterion role map is transcribed exactly (zero `definitional` residue, `role` non-optional), the three minimum-evidence floors match their approved chip sets and the mechanically-constructed §3.1 `CLUSTER_C_DEFINING_CHIPS` set is clinically defensible (gates on the cluster-distinguishing constellation, neither over- nor under-inclusive), and the three safety invariants hold without exception — `exclusionReason` reuses existing label text and authors no new clinical sentence (4/5/6); the engine output carries no band word, percentage, or diagnosis label (6); the SNNOOP10 red-flag backstop is byte-for-byte intact (9); and the NON-NEGOTIABLE aura-laterality rewire is correct, with `loc-unilateral` surviving only where headache-pain laterality is genuinely meant and `aura-symptom-unilateral` driving the §1.2 C item 4 count. The EMIT/DROP split, the vm-history drop + vm-B relabel, the H6 inversion, and the dev-time suppression-path invariant (dual-implemented in engine and test) all match the rulings. No drift in any never-drift category was introduced — recommendation strength, action verbs, qualifiers, certainty markers, and temporal constraints are all preserved, because this PR changes engine *control flow and chip vocabulary*, not any claim *text*. The one residual I cannot personally close — verbatim §1.5/§3.5/A1.6.6-B guideline text — was WebFetch-verified at authoring and ratified at the pre-execution gate, and is explicitly out of scope for a read-only post-execution pass. Approve.

## Required follow-ups

- None blocking. The Phase-1 deferrals confirmed at the pre-execution gate remain logged and are correctly not implemented here: (1) the §3.5.2/§3.5.4 indomethacin-pending near-miss stays behind the `hiddenUntilTrial` hard-hide (condition 3) — verified the gate is intact and PH/HC remain hidden without `indo-tried-complete`; (2) the full Bárány/Lempert-2012 5-criterion VM expansion stays `blocked:source-not-resolved` and out of scope.
- Stage Two (separate task) owns: result-screen band words, the "considered and set aside — [reason]" tray copy that consumes `definitionallyExcluded` + `exclusionReason`, the "see Chronic TTH" steer for the cm-C EMIT case, and the non-collapsible SNNOOP10 secondary-cause render on every result. None of these are engine concerns and none are in this diff — correct.
- Standard Class E gate sequencing: this artifact is the post-execution clinical pass required before `/pr-ready`. The orchestrator writes it to `docs/reviews/clinical-headache-engine-rank-and-flag-postexec.md`, then runs the quality gates (tsc, build, claims hook, the bifurcated test suite).
