# HTML Mockups

Each section spec has one or more HTML mockup files here. These use real Tailwind tokens and are the visual source of truth.

Claude Code reads the HTML directly when implementing JSX — no translation layer, no drift between image and code.

## Files
[Will be populated as specs are drafted.]

## How to view
Open any .html file in a browser. These are standalone files — they include their own Tailwind CDN link and work offline.
